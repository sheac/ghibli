# Studio Ghibli Film Board

## Prereqs

* Memcached installed

## Startup

```
./run_server.sh
```

Then to access, hit `127.0.0.1:8000/movies`. The response is JSON formatted.

## Run tests

```
./run_tests.sh
```